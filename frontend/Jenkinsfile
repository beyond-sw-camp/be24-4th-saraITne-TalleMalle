pipeline {
  agent {
    kubernetes {
      label "node-kaniko-${UUID.randomUUID().toString()}"
      namespace "jdh"
      defaultContainer 'node'
      yaml """
apiVersion: v1
kind: Pod
metadata:
  annotations:
    sidecar.istio.io/inject: "false"
spec:
  restartPolicy: Never
  containers:
    - name: node
      image: node:22.17.1-bullseye
      command: ['cat']
      tty: true
    - name: kaniko
      image: gcr.io/kaniko-project/executor:debug
      command: ['/busybox/sh','-c','sleep infinity']
      tty: true
      volumeMounts:
        - name: docker-config
          mountPath: /kaniko/.docker
        - name: workspace
          mountPath: /workspace
    - name: kubectl
      image: bitnami/kubectl:latest
      command: ['sleep', 'infinity']
      tty: true
      securityContext:
        runAsUser: 0
  volumes:
    - name: workspace
      emptyDir: {}
    - name: docker-config
      secret:
        secretName: dockerhub-cred1
        items:
          - key: .dockerconfigjson
            path: config.json
"""
    }
  }

  environment {
    IMAGE_NAME     = 'deatytim/tmfront'
    IMAGE_TAG      = "${env.BUILD_NUMBER}"
    NAMESPACE      = 'jdh'
    SVC_NAME       = 'frontend-service'
    CONTAINER_NAME = 'frontend'
  }

  stages {
    stage('Checkout') {
      steps {
        container('node') {
          checkout scm
        }
      }
    }

    stage('Setup K8s Resources') {
      steps {
        container('kubectl') {
          sh """
            kubectl apply -f ${WORKSPACE}/k8s/06-frontend-deployment.yaml -n ${NAMESPACE}
            kubectl apply -f ${WORKSPACE}/k8s/06-frontend-green-deployment.yaml -n ${NAMESPACE}
            kubectl apply -f ${WORKSPACE}/k8s/07-frontend-service.yaml -n ${NAMESPACE}
            kubectl apply -f ${WORKSPACE}/k8s/08-tls-secret.yaml -n ${NAMESPACE}
            kubectl apply -f ${WORKSPACE}/k8s/09-frontend-ingress.yaml -n ${NAMESPACE}
          """
        }
      }
    }

    stage('npm Install & Build') {
      steps {
        container('node') {
          dir('frontend') {
            sh '''
              npm install
              npm run build
            '''
          }
        }
      }
    }

    stage('Kaniko Build & Push') {
      steps {
        container('kaniko') {
          sh """
            /kaniko/executor \\
              --context=${WORKSPACE}/frontend \\
              --dockerfile=${WORKSPACE}/frontend/Dockerfile \\
              --destination=${IMAGE_NAME}:${IMAGE_TAG} \\
              --destination=${IMAGE_NAME}:latest \\
              --single-snapshot \\
              --use-new-run \\
              --cache=true \\
              --snapshotMode=redo
          """
        }
      }
    }

    stage('Blue/Green Deploy') {
      steps {
        container('kubectl') {
          script {
            // 현재 서비스가 가리키는 slot 확인
            def activeSlot = sh(
              script: "kubectl get svc ${SVC_NAME} -n ${NAMESPACE} -o jsonpath='{.spec.selector.slot}'",
              returnStdout: true
            ).trim()

            // active가 blue면 green에 배포, green이면 blue에 배포
            def newSlot   = (activeSlot == 'blue') ? 'green' : 'blue'
            def newDeploy = "frontend-${newSlot}-deployment"

            echo "현재 active slot: ${activeSlot} → 배포 대상: ${newSlot}"

            // inactive deployment에 새 이미지 배포 및 롤아웃 대기
            sh """
              kubectl set image deployment/${newDeploy} \\
                ${CONTAINER_NAME}=${IMAGE_NAME}:${IMAGE_TAG} \\
                -n ${NAMESPACE}

              kubectl rollout status deployment/${newDeploy} \\
                -n ${NAMESPACE} --timeout=180s
            """

            // 롤아웃 성공 → 서비스 트래픽을 새 slot으로 전환
            sh """
              kubectl patch svc ${SVC_NAME} -n ${NAMESPACE} \\
                -p '{"spec":{"selector":{"type":"frontend","slot":"${newSlot}"}}}'
            """

            echo "Blue/Green 전환 완료: ${activeSlot} → ${newSlot}"
          }
        }
      }
    }
  }

  post {
    success {
      echo "Frontend 배포 완료: ${IMAGE_NAME}:${IMAGE_TAG}"
    }
    failure {
      echo "Frontend 배포 실패 — 서비스는 기존 slot 유지"
    }
  }
}
